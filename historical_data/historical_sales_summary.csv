import os
import pandas as pd

def generate_monthly_summary():
    input_file = "historical_sales.csv.gz"
    output_file = "historical_sales_summary.csv"

    print("⏳ Step 1/3: Reading raw compressed dataset...")
    df = pd.read_csv(input_file, compression='gzip', low_memory=False)
    df.columns = [str(c).strip() for c in df.columns]

    branch_col = next((c for c in df.columns if 'branch' in c.lower()), None)
    net_col = next((c for c in df.columns if 'net' in c.lower()), None)
    date_col = next((c for c in df.columns if 'date' in c.lower()), None)

    df[net_col] = pd.to_numeric(df[net_col], errors='coerce').fillna(0)
    df[date_col] = pd.to_datetime(df[date_col], errors='coerce')
    df = df.dropna(subset=[date_col])

    # Extract Year and Month (Combines ~30 days into 1 row per store)
    df['Year'] = df[date_col].dt.year
    df['Month'] = df[date_col].dt.month

    group_cols = [branch_col, 'Year', 'Month']
    if 'Source' in df.columns: group_cols.append('Source')
    if 'Brand Name' in df.columns: group_cols.append('Brand Name')

    print("📊 Step 2/3: Grouping by Month & Store...")
    summary_df = df.groupby(group_cols)[net_col].sum().reset_index()

    print("💾 Step 3/3: Saving ultra-compressed CSV...")
    summary_df.to_csv(output_file, index=False)
    
    file_size_mb = os.path.getsize(output_file) / (1024 * 1024)
    print(f"\n✅ DONE! Optimized CSV created: '{output_file}' ({file_size_mb:.2f} MB)")

if __name__ == "__main__":
    generate_monthly_summary()
